import React from 'react';
import ShowForm from '../components/ShowForm/ShowForm';

const UpdateShow = () => {
  return (
    <section>
      UpdateShow
      <ShowForm action={'Update Show'} />
    </section>
  );
};

export default UpdateShow;
